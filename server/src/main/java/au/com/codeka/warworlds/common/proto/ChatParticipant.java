// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: au/com/codeka/warworlds/common/proto/chat.proto at 42:1
package au.com.codeka.warworlds.common.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * Refers to a participant of a chat room.
 */
public final class ChatParticipant extends Message<ChatParticipant, ChatParticipant.Builder> {
  public static final ProtoAdapter<ChatParticipant> ADAPTER = new ProtoAdapter_ChatParticipant();

  private static final long serialVersionUID = 0L;

  public static final Long DEFAULT_EMPIRE_ID = 0L;

  public static final Boolean DEFAULT_IS_MUTED = false;

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#INT64"
  )
  public final Long empire_id;

  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean is_muted;

  public ChatParticipant(Long empire_id, Boolean is_muted) {
    this(empire_id, is_muted, ByteString.EMPTY);
  }

  public ChatParticipant(Long empire_id, Boolean is_muted, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.empire_id = empire_id;
    this.is_muted = is_muted;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.empire_id = empire_id;
    builder.is_muted = is_muted;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ChatParticipant)) return false;
    ChatParticipant o = (ChatParticipant) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(empire_id, o.empire_id)
        && Internal.equals(is_muted, o.is_muted);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (empire_id != null ? empire_id.hashCode() : 0);
      result = result * 37 + (is_muted != null ? is_muted.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (empire_id != null) builder.append(", empire_id=").append(empire_id);
    if (is_muted != null) builder.append(", is_muted=").append(is_muted);
    return builder.replace(0, 2, "ChatParticipant{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<ChatParticipant, Builder> {
    public Long empire_id;

    public Boolean is_muted;

    public Builder() {
    }

    public Builder empire_id(Long empire_id) {
      this.empire_id = empire_id;
      return this;
    }

    public Builder is_muted(Boolean is_muted) {
      this.is_muted = is_muted;
      return this;
    }

    @Override
    public ChatParticipant build() {
      return new ChatParticipant(empire_id, is_muted, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_ChatParticipant extends ProtoAdapter<ChatParticipant> {
    ProtoAdapter_ChatParticipant() {
      super(FieldEncoding.LENGTH_DELIMITED, ChatParticipant.class);
    }

    @Override
    public int encodedSize(ChatParticipant value) {
      return (value.empire_id != null ? ProtoAdapter.INT64.encodedSizeWithTag(1, value.empire_id) : 0)
          + (value.is_muted != null ? ProtoAdapter.BOOL.encodedSizeWithTag(2, value.is_muted) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, ChatParticipant value) throws IOException {
      if (value.empire_id != null) ProtoAdapter.INT64.encodeWithTag(writer, 1, value.empire_id);
      if (value.is_muted != null) ProtoAdapter.BOOL.encodeWithTag(writer, 2, value.is_muted);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public ChatParticipant decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.empire_id(ProtoAdapter.INT64.decode(reader)); break;
          case 2: builder.is_muted(ProtoAdapter.BOOL.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public ChatParticipant redact(ChatParticipant value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
