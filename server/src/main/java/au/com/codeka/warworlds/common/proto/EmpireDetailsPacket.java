// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: au/com/codeka/warworlds/common/proto/packets.proto at 66:1
package au.com.codeka.warworlds.common.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * The response to a RequestEmpirePacket, contains details of the empire(s) you requested.
 */
public final class EmpireDetailsPacket extends Message<EmpireDetailsPacket, EmpireDetailsPacket.Builder> {
  public static final ProtoAdapter<EmpireDetailsPacket> ADAPTER = new ProtoAdapter_EmpireDetailsPacket();

  private static final long serialVersionUID = 0L;

  /**
   * The empire(s) that you requested in RequestEmpirePacket.
   */
  @WireField(
      tag = 1,
      adapter = "au.com.codeka.warworlds.common.proto.Empire#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<Empire> empires;

  public EmpireDetailsPacket(List<Empire> empires) {
    this(empires, ByteString.EMPTY);
  }

  public EmpireDetailsPacket(List<Empire> empires, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.empires = Internal.immutableCopyOf("empires", empires);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.empires = Internal.copyOf("empires", empires);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof EmpireDetailsPacket)) return false;
    EmpireDetailsPacket o = (EmpireDetailsPacket) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(empires, o.empires);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (empires != null ? empires.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (empires != null) builder.append(", empires=").append(empires);
    return builder.replace(0, 2, "EmpireDetailsPacket{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<EmpireDetailsPacket, Builder> {
    public List<Empire> empires;

    public Builder() {
      empires = Internal.newMutableList();
    }

    /**
     * The empire(s) that you requested in RequestEmpirePacket.
     */
    public Builder empires(List<Empire> empires) {
      Internal.checkElementsNotNull(empires);
      this.empires = empires;
      return this;
    }

    @Override
    public EmpireDetailsPacket build() {
      return new EmpireDetailsPacket(empires, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_EmpireDetailsPacket extends ProtoAdapter<EmpireDetailsPacket> {
    ProtoAdapter_EmpireDetailsPacket() {
      super(FieldEncoding.LENGTH_DELIMITED, EmpireDetailsPacket.class);
    }

    @Override
    public int encodedSize(EmpireDetailsPacket value) {
      return Empire.ADAPTER.asRepeated().encodedSizeWithTag(1, value.empires)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, EmpireDetailsPacket value) throws IOException {
      if (value.empires != null) Empire.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.empires);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public EmpireDetailsPacket decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.empires.add(Empire.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public EmpireDetailsPacket redact(EmpireDetailsPacket value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.empires, Empire.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
