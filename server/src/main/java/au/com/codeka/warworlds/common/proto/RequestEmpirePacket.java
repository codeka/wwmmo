// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: au/com/codeka/warworlds/common/proto/packets.proto at 60:1
package au.com.codeka.warworlds.common.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * Requests details about an empire, or empires.
 */
public final class RequestEmpirePacket extends Message<RequestEmpirePacket, RequestEmpirePacket.Builder> {
  public static final ProtoAdapter<RequestEmpirePacket> ADAPTER = new ProtoAdapter_RequestEmpirePacket();

  private static final long serialVersionUID = 0L;

  /**
   * The ID of the empire you want details of. Required.
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#INT64",
      label = WireField.Label.REPEATED
  )
  public final List<Long> empire_id;

  public RequestEmpirePacket(List<Long> empire_id) {
    this(empire_id, ByteString.EMPTY);
  }

  public RequestEmpirePacket(List<Long> empire_id, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.empire_id = Internal.immutableCopyOf("empire_id", empire_id);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.empire_id = Internal.copyOf("empire_id", empire_id);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RequestEmpirePacket)) return false;
    RequestEmpirePacket o = (RequestEmpirePacket) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(empire_id, o.empire_id);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (empire_id != null ? empire_id.hashCode() : 1);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (empire_id != null) builder.append(", empire_id=").append(empire_id);
    return builder.replace(0, 2, "RequestEmpirePacket{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RequestEmpirePacket, Builder> {
    public List<Long> empire_id;

    public Builder() {
      empire_id = Internal.newMutableList();
    }

    /**
     * The ID of the empire you want details of. Required.
     */
    public Builder empire_id(List<Long> empire_id) {
      Internal.checkElementsNotNull(empire_id);
      this.empire_id = empire_id;
      return this;
    }

    @Override
    public RequestEmpirePacket build() {
      return new RequestEmpirePacket(empire_id, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RequestEmpirePacket extends ProtoAdapter<RequestEmpirePacket> {
    ProtoAdapter_RequestEmpirePacket() {
      super(FieldEncoding.LENGTH_DELIMITED, RequestEmpirePacket.class);
    }

    @Override
    public int encodedSize(RequestEmpirePacket value) {
      return ProtoAdapter.INT64.asRepeated().encodedSizeWithTag(1, value.empire_id)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RequestEmpirePacket value) throws IOException {
      if (value.empire_id != null) ProtoAdapter.INT64.asRepeated().encodeWithTag(writer, 1, value.empire_id);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RequestEmpirePacket decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.empire_id.add(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RequestEmpirePacket redact(RequestEmpirePacket value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
