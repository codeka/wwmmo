// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: common.proto at 12:1
package au.com.codeka.warworlds.common.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * Contains details about an in-app purchase, used by various requests which require in-app purchase
 * information.
 */
public final class PurchaseInfo extends Message<PurchaseInfo, PurchaseInfo.Builder> {
  public static final ProtoAdapter<PurchaseInfo> ADAPTER = new ProtoAdapter_PurchaseInfo();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_SKU = "";

  public static final String DEFAULT_TOKEN = "";

  public static final String DEFAULT_ORDER_ID = "";

  public static final String DEFAULT_PRICE = "";

  public static final String DEFAULT_DEVELOPER_PAYLOAD = "";

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String sku;

  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String token;

  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String order_id;

  @WireField(
      tag = 4,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String price;

  @WireField(
      tag = 5,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String developer_payload;

  public PurchaseInfo(String sku, String token, String order_id, String price, String developer_payload) {
    this(sku, token, order_id, price, developer_payload, ByteString.EMPTY);
  }

  public PurchaseInfo(String sku, String token, String order_id, String price, String developer_payload, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.sku = sku;
    this.token = token;
    this.order_id = order_id;
    this.price = price;
    this.developer_payload = developer_payload;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.sku = sku;
    builder.token = token;
    builder.order_id = order_id;
    builder.price = price;
    builder.developer_payload = developer_payload;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof PurchaseInfo)) return false;
    PurchaseInfo o = (PurchaseInfo) other;
    return Internal.equals(unknownFields(), o.unknownFields())
        && Internal.equals(sku, o.sku)
        && Internal.equals(token, o.token)
        && Internal.equals(order_id, o.order_id)
        && Internal.equals(price, o.price)
        && Internal.equals(developer_payload, o.developer_payload);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (sku != null ? sku.hashCode() : 0);
      result = result * 37 + (token != null ? token.hashCode() : 0);
      result = result * 37 + (order_id != null ? order_id.hashCode() : 0);
      result = result * 37 + (price != null ? price.hashCode() : 0);
      result = result * 37 + (developer_payload != null ? developer_payload.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (sku != null) builder.append(", sku=").append(sku);
    if (token != null) builder.append(", token=").append(token);
    if (order_id != null) builder.append(", order_id=").append(order_id);
    if (price != null) builder.append(", price=").append(price);
    if (developer_payload != null) builder.append(", developer_payload=").append(developer_payload);
    return builder.replace(0, 2, "PurchaseInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PurchaseInfo, Builder> {
    public String sku;

    public String token;

    public String order_id;

    public String price;

    public String developer_payload;

    public Builder() {
    }

    public Builder sku(String sku) {
      this.sku = sku;
      return this;
    }

    public Builder token(String token) {
      this.token = token;
      return this;
    }

    public Builder order_id(String order_id) {
      this.order_id = order_id;
      return this;
    }

    public Builder price(String price) {
      this.price = price;
      return this;
    }

    public Builder developer_payload(String developer_payload) {
      this.developer_payload = developer_payload;
      return this;
    }

    @Override
    public PurchaseInfo build() {
      return new PurchaseInfo(sku, token, order_id, price, developer_payload, buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PurchaseInfo extends ProtoAdapter<PurchaseInfo> {
    ProtoAdapter_PurchaseInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, PurchaseInfo.class);
    }

    @Override
    public int encodedSize(PurchaseInfo value) {
      return (value.sku != null ? ProtoAdapter.STRING.encodedSizeWithTag(1, value.sku) : 0)
          + (value.token != null ? ProtoAdapter.STRING.encodedSizeWithTag(2, value.token) : 0)
          + (value.order_id != null ? ProtoAdapter.STRING.encodedSizeWithTag(3, value.order_id) : 0)
          + (value.price != null ? ProtoAdapter.STRING.encodedSizeWithTag(4, value.price) : 0)
          + (value.developer_payload != null ? ProtoAdapter.STRING.encodedSizeWithTag(5, value.developer_payload) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, PurchaseInfo value) throws IOException {
      if (value.sku != null) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.sku);
      if (value.token != null) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.token);
      if (value.order_id != null) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.order_id);
      if (value.price != null) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.price);
      if (value.developer_payload != null) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.developer_payload);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PurchaseInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.sku(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.token(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.order_id(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.price(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.developer_payload(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public PurchaseInfo redact(PurchaseInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
