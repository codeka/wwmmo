package au.com.codeka.common.model;

public class BuildingEffect extends Design.Effect {
    /**
     * This is called when we're loaded, we need to apply whatever effect we have to the star,
     * colony or building.
     */
    public void apply(BaseStar star, BaseColony colony, BaseBuilding building) {
    }
}
